# Dependencies
node_modules/

# Builds
dist/
coverage/
*.tsbuildinfo
.vite/

# Environment — never commit secrets
.env
.env.*
!.env.example

# OS / editor noise
.DS_Store
*.log
logs/

# User-uploaded review photos (dev-grade local storage; prod uses a CDN)
apps/api/uploads/

# Local-only operational scripts (e.g. admin provisioning) — kept off the repo
apps/api/scripts/
